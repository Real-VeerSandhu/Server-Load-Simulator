[package]
name = "queue_engine"
version = "0.1.0"
edition = "2021"

[lib]
name = "queue_engine"
crate-type = ["cdylib"]

[dependencies]
rand = "0.8"
serde = { version = "1.0", features = ["derive"] }
