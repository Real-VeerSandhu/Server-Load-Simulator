simulation:
  servers: 3
  arrival_rate: 2.0
  processing_time: 1.0
  processing_variance: 0.3

display:
  refresh_rate: 10  # Hz
  chart_width: 60
  show_histograms: true

simulator:
  binary_path: "./rust-engine/target/release/simulator"
  max_queue_size: 50
